Este problema toma base numa matriz 3x3, e pede que encontremos o maior índice i para que a função kg(fi(g)) seja ﬁnita. Basicamente, esse índice aumenta de forma corresponde `a quantidade de vezes que podemos aplicar a função f() nessa matriz 3x3, até que todos os elementos nela se tornem 0. Nota-se que o índice i começa em -1, pois ´e a resposta dada caso todos os elementos da matriz 3x3 fornecida sejam 0. A função f() descrita no problema serve para transformar cada elemento da matriz 3x3 num elemento de fórmula NovoElemento = (SomaDosV izinhos)mod(2). Como sempre iremos trabalhar com uma matriz 3x3, a posição dos vizinhos de cada elemento (e consequentemente a fórmula da função f) não mudara´. Nas linhas 1-14 temos essa função f, que calcula a fórmula dada em cada elemento, e retorna uma matriz resultado. ´E uma solução simples, ou seja, atende apenas esse problema em especíﬁco. Na função principal, começamos com uma matriz de entrada vazia 3x3 (linha 17), um array para guardar os resultados (linha 19), e o número de testes que o usuário deseja
fazer (linha 18). Enquanto todos os testes não forem executados (linha 21), recebemos uma linha vazia (´e o espaçamento entre o número de testes e os valores da matriz, como os exemplos de input da questão mostram), e recebemos também os valores de cada linha da matriz (linha 24), atribuindo esses valores a` matriz (linhas 25-27). Com a matriz devidamente montada, começamos a calcular a resposta. Começamos com a resposta = -1 (linha 29), como ja´ discutido, e iniciamos um loop (linha 30) enquanto a matriz 3x3 não for composta apenas por elementos 0. A veriﬁcação dessa condição se da´ nas linhas 31-35, onde percorremos a matriz veriﬁcando se algum valor não ´e 0 (linha 34), caso todos sejam 0, saímos do loop (linha 35) e paramos a operação, caso contrário, aplicamos a função f na matriz de entrada (linha 36) e incrementamos uma unidade `a resposta (linha 37). Quando a condição de parada for alcançada, saímos do loop da linha 30 e colocamos o resultado no array de respostas (linha 39). Quando o usuário quiser parar os testes, o loop da linha 21 se encerrara´ e printamos as respostas coletadas (linha 41).
