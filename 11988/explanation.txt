O problema descreve um teclado quebrado, no qual pressionar a tecla ”[”volta o iterador do texto para o começo do texto, e ”]”avança para o ﬁnal. O objetivo do programa ´e mostrar o texto resultante da entrada do usuário dadas essas circunstâncias. Como sugerido, utilizamos a biblioteca list do C++, que já nos proporciona exatamente com o que precisamos, os m´métodos end() e begin(). Primeiramente, criamos o array texto para receber o texto do usuário (linha 8). Começamos então o loop da linha 11, que continua até que o usuário cancele a operação. Aqui, declaramos uma lista saída (linha 13), e um iterador chamado texto-agr (linha 14), necessário
17
para que possamos utilizar os m´métodos end() e begin(). Inicializamos esse iterador ao começo da lista na linha 15 (deu erro ao tentar inicializar nulo), e limpamos os dados desse iterador para poder começar as operações (linha 16). Começamos então um loop que percorre o texto fornecido pelo usuário (linha 18), e a cada letra checamos se o usuário utilizou algumas das teclas quebradas, caso as tenha usado, o iterador se posiciona de acordo (linhas 19-20), caso contrário, escreve onde esta´ (linha 21). Finalmente, printamos o texto resultante (Créditos para função de Print.)
